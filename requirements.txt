Flask==2.3.3                     # For the Flask web framework
Flask-WTF==1.0.1                 # For form handling (Contact page)
Flask-Mail==0.9.1                # For sending emails (Contact page functionality)
tensorflow==2.14.0               # For deep learning models
numpy==1.23.5                    # For numerical computations
scikit-image==0.20.0             # For image preprocessing (ML models)
scikit-learn==1.3.0              # For machine learning models
joblib==1.3.1                    # For loading serialized models
Werkzeug==2.3.7                  # Flask dependency for routing
gunicorn==21.1.0                 # For production server deployment (optional for local setup)
pillow==10.0.0                   # For image loading and manipulation
matplotlib==3.8.0                # Optional: For visualization
email-validator==2.1.0           # For email validation in forms
